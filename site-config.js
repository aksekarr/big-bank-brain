/*
 * Presentation-only setting. Change this after manual review; it has no effect on
 * the pipeline, its verification records, or the underlying briefing data.
 */
window.BBB_CONFIG = {
  review_status: "reviewed"
};
